name: assignment_two_flutter_ui_koushik_sarker_2022863
description: "Flutter UI Assignment 2 - Banking & Profile App for Koushik Sarker (ID: 2022863)"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
